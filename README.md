# SensorPage D3
This is the webpage template used to build output webpage from SensorReaderService.

It uses JavaScript along with D3. The version of D3 used by the webpage is included in the file d3.v7.min.js.

Visual Studio is used to create/edit the assets. F12/Developer-tools in Edge browser are used to debug.